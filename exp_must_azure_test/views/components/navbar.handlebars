<nav class="bg-slate-900 border-gray-200 px-2 sm:px-4 py-2.5 rounded dark:bg-gray-900">
      <div>
        <a href="/" >Eashire</a>
        <button type="button" data-toggle="collapse" data-target="#navbarCollapse"
          aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
          <span></span>
        </button>
        <div id="navbarCollapse">
          <ul>
            <li>
              <a href="/" class="text-black{{#if active.home}} active{{/if}}">Home</a>
            </li>
            <li>
              <a href="/ficheposte">
                Fiche de poste
              </a>
            </li>
            {{#if user}}
              <li data-turbolinks="false">
                <a href="/calendar" class="{{#if active.calendar}} active{{/if}}">Calendar</a>
              </li>
              <li >
                <a href="/dashboard" class="{{#if active.dashboard}} active{{/if}}">
                  Dashboard
                </a>
              </li>
            {{/if}}
          </ul>
          <ul>
            {{#if user}}
              <li>
                <a data-toggle="dropdown" href="#" role="button" aria-haspopup="true"
                  aria-expanded="false">
                  {{#if user.avatar}}
                    <img src="{{ user.avatar }}" style="width: 32px;">
                  {{else}}
                    <i style="width: 32px;"></i>
                  {{/if}}
                </a>
                <div >
                  <h5>{{ user.displayName }}</h5>
                  <p>{{ user.email }}</p>
                  <div></div>
                  <a href="/auth/signout">Sign Out</a>
                </div>
              </li>
            {{else}}
              <li>
                <a href="/auth/signin">Sign In</a>
              </li>
            {{/if}}
          </ul>
        </div>
      </div>
    </nav>